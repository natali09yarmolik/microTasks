import React from "react";
/*import {arrayMoney} from "../App";
import {banknotsType} from '../App'*/

/*type buttonValuePropsType={
    arrayMoney: Array<arrayMoney>
    filterMoney:(banknot:banknotsType)=>void
}

export const ButtonValue = (props: buttonValuePropsType) => {

    return (<div>
        <ul>
            {props.arrayMoney.map((money, index) => {
                return <li key={index}>{money.banknots} {money.value} {money.number}</li>
            })}
        </ul>

            <button onClick={() => props.filterMoney('Dollars')}>Dollar</button>
            <button onClick={() => props.filterMoney('RUBLS')}>RUBLS</button>
            <button onClick={() => props.filterMoney('All')}>All</button>
</div>
    )
};*/
